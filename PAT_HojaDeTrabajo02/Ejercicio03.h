#pragma once

#include <string>
#inclide <vector>
using std::string;

class Ejercicio03
{
public:
	void sortl(string s);
	string opera(char ope, string N1, string N2);
	int opsearch(vector<string>orig);
	vector<string> operando();
	int calculate(string s);
};

